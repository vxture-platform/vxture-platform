import { PlanPublishingListPage } from "@/modules/products/PlanPublishingListPage";

/* 一级列表：每行一个产品（90-plan-publishing.md 重做稿 §1）。
   旧的「产品 × 五档矩阵 + 版本时间线 + 编辑器」单页移到 /plan-versions/editor
   作过渡，待草稿编辑器那一批把它整体替换掉。 */
export default function Page() {
  return <PlanPublishingListPage />;
}
