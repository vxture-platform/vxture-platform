#!/usr/bin/env node
// ─────────────────────────────────────────────────────────────────────────────
// i18n 铺开进度的**棘轮**：硬编码中文串只许变少，不许变多
//
// 机制层已经统一（五个门户都在 next-intl 上），但铺开是个跨多次会话的活——
// 6000 多条串、150 多个文件。这种长跑最典型的失败方式不是「做得慢」，是
// **一边抽一边加**：这周抽掉两百条，下周新页面又写进去三百条，半年后总数
// 没动，而每个人都觉得自己在推进。
//
// 所以这里不设目标、只设棘轮：每个门户记一个当前数，**超过就报错**。抽完一批
// 就把数字调下去，那一步是提交的一部分，改不改得动一目了然。
//
// ## 判据：代码位置，不是文件
//
// 这个仓的注释全是中文，按「含中文的文件数」统计会被污染得毫无意义（opera
// 58/58 个文件都含中文，但那大半是注释）。所以先把注释挖空（保持行号），再数
// 三类：双引号/反引号字面量、以及 JSX 文本节点。
//
// 会有少量误判——比如 `RISK_LEVEL_META` 里 `label: "read"` 那种技术标识不该翻译，
// 但它本来就不含中文，不进统计。真正的误判是「注释里带引号的中文被算进代码」，
// 挖空注释已经处理掉了。宁可略高估，不可低估：低估会让棘轮松掉。
//
// 运行：  node scripts/guardrails/check-i18n-coverage.mjs
// 别名：  pnpm lint:i18n-coverage
// 退出码：任一门户超过基线 → 1。
// ─────────────────────────────────────────────────────────────────────────────

import { readFileSync, readdirSync, statSync, existsSync } from "node:fs";
import { join, resolve, extname } from "node:path";
import { fileURLToPath } from "node:url";

const REPO_ROOT = resolve(fileURLToPath(new URL("../../", import.meta.url)));
const PORTALS_DIR = join(REPO_ROOT, "portals");

/**
 * 每个门户当前的硬编码中文串数。**这是棘轮，只许调小。**
 *
 * 抽完一批之后把数字改成新的实测值，和代码改动在同一个提交里。调大它需要一个
 * 理由，而写下那个理由的时候多半会发现不该调大。
 *
 * 2026-08-26 基线（i18n 基座落地时实测）：
 *   console / website 已基本抽完；admin 与 opera 是主战场；accounts 抽了一条竖切。
 * 2026-08-26 opera 2106 → 1826：抽掉跨文件重复的通用词汇（列表页外壳、通用动作、
 *   列头、状态词）一批 280 处 / 22 个文件。
 * 2026-08-26 admin 4125 → 3844：同一套共享命名空间的扫描，281 处 / 43 个文件。
 *   两个门户的 `common` / `columns` / `filters` / `actions` / `status.generic` 形状
 *   刻意一致——同一个词在两边该是同一个键。
 * 2026-08-26 第二轮扫中低频重复：opera 1826 → 1795（31 处），admin 3844 → 3799
 *   （45 处）。剩下的跨文件重复主要是三类，都不该机械扫：导航标签（结构归
 *   `opera-navigation-design.md` 裁）、领域名词（要先有词表）、以及宿主不是组件
 *   的那些（模块级常量表 / 普通函数，要先做结构改造）。
 * 2026-08-26 admin 3799 → 3756：五个「状态 → 中文」的顶层函数删掉，改成词条查表
 *   （映射本来就是恒等，函数没有任何判断可言）。
 * 2026-08-26 admin 3756 → 3668：PlatformGovernanceListPage 的四套治理域配置搬进
 *   词条，图标与语气留在代码里（同 opera `status.ts` 的判据）。
 * 2026-08-27 admin 3668 → 3660：视觉验证时抓到的混合语言（`还没有${objectLabel}`
 *   在英文下渲染成「还没有Secret」），连同两处同形的一起抽掉。
 * 2026-08-27 admin 3660 → 3666：**真新增，不是口径问题。** 技能、公告、服务
 *   套餐三页的手搓卡片换 `MetricListCard` 时，它的 `metrics` 契约要求**每个读数
 *   带一个标签**，而原来那排 meta 是一串裸值（「分类」v1.2「123 次调用」混在
 *   一行）。于是多出七个标签词：分类 / 版本 / 调用次数 / 发布范围 / 发布时间 /
 *   未发布 / 基础套餐。这是真的多写了文案，基线往上调；它们该进词条，归入
 *   i18n 那条线的待办。
 *
 * 2026-08-27 admin 3662 → 3660：TenantDetailPage 换 DS 件时又抵回去了：多处
 *   `<small>注册激活</small>` 这类不带插值的 JSX 文本节点（本来就被数到）变成了
 *   `label` 属性串，一进一出持平；而几个重复的标题段被 DS 件合并后真的少了。
 *
 * 2026-08-27 admin 3660 → 3662：同一个口径问题第三次出现。五个手搓模态换
 *   `DialogForm` 时，两处副标题从 `<p>{bill.billNo} · 当前应收{" "}{fmt(…)}</p>`
 *   变成了 `description` 的模板串。**串一直都在**，只是之前没被数到。
 *
 *   根因是本文件的盲点：`JSX_TEXT` 的 `[^<>{}
]*` 在碰到插值时就断了，
 *   所以「带插值的 JSX 文本节点」一律漏数。把它补上会一次性抬高五个门户的
 *   基线，属于 i18n 那条线的活，不该搭在 CSS 改造里做——先如实记在这里。
 *
 * 2026-08-27 admin 3661 → 3660：卡片换 `MetricListCard` 后又自己抵回来了——
 *   `__metrics` 里的 `<small>配额消耗</small>` 这类 JSX 文本节点变成了 `label`
 *   属性串，而它们本来就被 `JSX_TEXT` 数到过，一进一出刚好抵掉下面那一条。
 *
 * 2026-08-27 admin 3660 → 3661（已被上一条抵消）：**计数口径变了，不是多写了一条。** CommerceOverviewPage
 *   的「个订阅」原本是 `<small>{formatNumber(n)} 个订阅</small>`——JSX 文本节点里
 *   带插值时 `JSX_TEXT` 的 `[^<>{}
]*` 匆匆跑掉，一直没被数到；手搜网格换成
 *   `TableTitleCell` 后它成了 `description` 的模板串，于是被 `LITERAL` 数到了。
 *   这正是本文件开头说的「宁可略高估」：高估修正一次，比为了压住数字把代码写回
 *   正则看不见的形状好。
 *
 * 2026-08-29 opera 1795 → 1818：`model/services` 页新增「低谷定价」一节
 *   （TD-047 / atlas#47：provider 的 `config.pricing.offPeak` 此前在界面上无处可设）。
 *   **没有走 `t()`，是有意的。** 这一页 280 条硬编码中文一条都还没抽，是本门户里
 *   最大的一处；只把新增的这一节抽出去，得到的是一页半中英——而那正是 2026-08-27
 *   视觉走查在 admin 上抓到的缺陷（局部抽取造出的混合语言）。抽要连整页抽，
 *   那是 i18n 那条线的活，不该搭在一个补配置入口的改动里做。
 *
 * 2026-08-29 admin 3666 → 3510：**不是这次改小的，是基线一直没跟上。**
 *   实测 3510，基线停在 3666，中间那 156 条的余量意味着棘轮这段时间根本没在咬——
 *   admin 可以悄悄多写 156 条硬编码而不被拦。本文件的全部作用就是不让它悄悄涨，
 *   所以余量本身就是缺陷。趁这次实测把它收到实数。
 *
 * 2026-08-29 accounts 263 → 264：登录表单在人机验证降级时多了一句
 *   「人机验证未完成（<code>），提交可能被拒绝」（B0 度量，见 70-workplan/90）。
 *   这一页（OidcLoginForm）本就全是硬编码中文、一条没抽，只抽这一句会造出半中英；
 *   抽要整页抽，那是 i18n 那条线的活。这句是把此前被吞掉的错误码放到用户眼前，
 *   不放就得继续靠猜。
 *
 * 2026-08-30 opera 1818 → 1819：`ops/health` 服务状态页清单改以 product.products
 *   为主表（opera/40-product-registry.md），于是多出一个产品级的中性态「未接入」
 *   （目录里有、没有任何 OIDC 客户端），以及探测点抽屉里对应的说明段。
 *   **没有走 `t()`，是有意的**：这一页全部文案都是硬编码中文、一条没抽，只抽新加的
 *   这几句会造出半中英（同 08-29 `model/services` 那条的判据）。同一个词四处出现的
 *   收成一个常量（与页内既有的 `LIVENESS_LABELS` 同一做法），所以净增只有 1。
 *
 * 2026-08-31 admin 3510 → 3488：摘掉四个永远为空的菜单项（40-menu.md 1.2.0）连带
 *   删除审批中心 / 平台密钥两页与它们共用的治理列表页，那三个文件的硬编码中文随文件
 *   一起消失。实测收到实数，余量不留。
 *
 * 2026-08-31 admin 3488 → 3266：产品板块去 mock（TD-029）重写了解决方案 / 服务套餐
 *   四页与模型授权页，文案整页抽进 `productCatalog` / `productSolutionsPage` /
 *   `productSolutionDetailPage` / `servicePlansPage` / `servicePlanDetailPage` 五个
 *   命名空间（zh-CN / en-US 同步）。棘轮随之收紧到实数（与上一条同日合入，数字
 *   是两处改动叠加后的实测）。
 *
 * 2026-08-30 website 54 → 40：官网上线前去 mock。删掉的 `StatsSection`（「10+ 企业
 *   客户 / 98.0% 满意度 / 2000+ 用户」，硬编码且无处渲染）带走了全部 14 条；/pricing
 *   改读 `GET /api/products/:code/plans` 后新增的三态文案与配额/功能词典全部走 `t()`，
 *   一条没进代码。
 *
 * 2026-08-31 admin 3266 → 3234：技能市场页改读 Runos 能力目录时整页重写，文案抽进
 *   `skillsPage` 命名空间（zh-CN / en-US 同步），空桩时代的硬编码随页一起消失。
 *
 * 2026-08-31 admin 3234 → 3225（rebase 后实测；分支上原为 3510 → 3501）：首页与外壳去 mock。首页摘掉"待接入 / 暂无数据"补位行、
 *   Token 调用量前三卡、周期系数拼出来的"版本更新 N 次"，少了几条硬编码；外壳的
 *   通知抽屉（原先两条写死的演示通知）改读投递台账、设置抽屉改读主题/密度状态，
 *   新增文案全走 `t()`（drawer.notifications.empty/channels/statuses），
 *   零新增硬编码。棘轮随实数收紧。
 *
 * 2026-09-01 admin 3226 → 3204：套餐发布台整页重建（产品 × 五档矩阵），全部文案
 *   进 `planVersionsPage` 命名空间（zh-CN / en-US 同步）；旧「套餐版本」页的硬编码
 *   随页一起消失，净 −22。棘轮随实数收紧。
 *
 * 2026-08-31 admin 3225 → 3226：订阅详情的方案归属改从 product.solution_plans 实算，
 *   来源枚举多了 "solution" 一档，对应标签「方案关联」是唯一新增的一条硬编码
 *   （与同一函数里既有的两条并列，抽 t() 应当三条一起抽，留给该页整体 i18n 时做）。
 *
 * 2026-08-30 opera 1819 → 1822：`settings` 页从一张凭空写出来的表单（五个字面量默认
 *   值、保存按钮 disabled、opera-bff 没有任何配置表与端点）改成只读的事实页：说明没有
 *   可配项，把服务状态探测（间隔 / 两类端点 / 策略）与上游模块挂载这几条由代码与规格
 *   定下的事实按出处列出。表单的 18 条换成事实页的 22 条；外壳去掉没有通知源的
 *   「告警通知」钮（−1），一进一出净增 3。**没有走 `t()`，是有意的**：外壳与这一页
 *   都是硬编码中文、一条没抽，只抽这几句会造出半中英（同 08-29 / 08-30 两条的判据）。
 *
 * 2026-08-30 console 32 → 30：去 mock 那批删掉了零引用的 `TenantPlaceholderPage`
 *   （它的「规划内容」标题与说明是仅剩的两条硬编码）。应用中心重写与不可用态
 *   全部走 `t()`，没有新增。
 *
 * 2026-08-31 opera 1822 → 1868：产品退役闸门与「未登记产品的授权」报表
 *   （`opera/40-product-registry.md` §2 / §6）。产品目录页多出退役被拒的两条 Banner
 *   （409 带条数与样本、502 说明检查没做成）与对应 toast；权益配置页多出一整节报表
 *   （标题、说明、两条读取失败的 Banner、四列表头、行动作与确认文案、两种空态）和
 *   目录外产品码的详情兜底 Banner；`lifecycle.ts` 退役后果句加了前置说明。
 *   **没有走 `t()`，是有意的**：这两页全部文案都是硬编码中文、一条没抽（权益配置页
 *   124 条是本门户第三多的），只抽新加的这一节会造出半中英（同 08-29 / 08-30 的判据）。
 *
 * 2026-08-31 opera 1868 → 1871：`ops/health` 服务状态页加第三个中性态「不适用」
 *   （`opera/20-service-monitor.md` §4）：client 型产品（回调是 loopback）不探测，
 *   存活 / 就绪两列各一条词条，外加明细「客户端产品，无服务面」。与同页既有的
 *   「未接入」「未配置」一样硬编码——本页词条表 `LIVENESS_LABELS` / `READINESS_LABELS`
 *   整表都是字面量，单抽三条会造出半中英（同 08-30 的判据）。
 *
 * 2026-08-31 opera 1871 → 1890：接入检查单 C2 / C3 改为自动验证（`opera/40-product-registry.md`
 *   §2 步 2）。`launch-checks.ts` 多出两项检查的标题、判据、通过 / 未通过的事实与下一步
 *   （对方接通后留下的两条痕迹：权益拉取、用量上报），上线页的几处「五项 / 平台观测
 *   不到」改口。**没有走 `t()`，是有意的**：上线页与检查函数全部文案都是硬编码中文、
 *   一条没抽，只抽新加的两项会造出半中英（同 08-29 / 08-30 / 08-31 的判据）。
 *
 * 2026-08-31 opera 1890 → 1916: 产品删除（两步软删除，owner 优先级）。产品目录页多出
 *   删除菜单项、两步删除的确认对话框（影响面预览：可删则摊开连带处理——软删套餐数、
 *   会被停用的登录客户端；不可删则列出客户足迹 / 上游授权、指向退役），以及删除落锤的
 *   两种 409 出口（上游授权→复用退役那套 Banner、客户足迹→toast）与「原因码→中文」
 *   映射表。**没有走 `t()`，是有意的**：产品目录页全部文案硬编码中文、一条没抽（131 条，
 *   本门户第三多），只抽新加的删除这一节会造出半中英（同 08-29 / 08-30 / 08-31 的判据）。
 *
 * 2026-08-31 admin 净零(3204 不变)：解决方案补删除给 `api/admin-bff.ts` 加了一条兜底
 *   错误文案「删除方案失败」(+1,与同文件既有错误串并列),同时 StepUpProvider 的验证码
 *   输入换成 InputOTP、去掉了硬编码 placeholder「6 位验证码」(−1),一进一出净零。
 *
 * 2026-09-01 admin 3204 → 3230(+26):产品目录(原产品能力)新增营销内容编辑框
 *   ProductContentEditDialog(成熟度/上站/双语营销字段)+ 改名文案,沿用产品模块既有的
 *   硬编码中文风格(productTypeLabel 等本就直返「平台/智能体」),整批暂缓 t() 化。
 *
 * 2026-09-02 admin 3230 → 2486(−744):三平面拆分 cutover —— 平台自治域治理页(身份权限/
 *   安全审计/系统配置/通知基座 10 页 + 模块)整体迁往 arche 治理平面,admin 侧删除,硬编码
 *   中文随文件一并撤走。棘轮同步下探到新水位。
 *
 * 2026-09-02 opera 1916 → 1930(+14):新增运营者「个人信息」自助页 app/(shell)/me/page.tsx
 *   (只读自视,入口在用户弹出面板)。opera 现仍是单语硬编码规制(i18n 铺开尚早),整页
 *   暂缓 t() 化,沿用既有风格;棘轮上探到新水位。
 *
 * 2026-09-02 Phase B(运营者写侧自助收敛到身份层):
 *   - admin 2486 → 2469(−17):退役内嵌邮箱自助 OperatorAccountSettings(硬编码中文随文件
 *     删除),/settings 改为账户中心跳转桥 AccountCenterBridge(新增少量硬编码),净减。
 *   - opera 1930 → 1933(+3):/me 加「前往账户中心」跳转区(单语硬编码规制,沿用既有风格)。
 *   - accounts 264 → 273(+9):新增 api/operator-self.ts 的接口层错误文案,沿用同门户
 *     api/operator-webauthn.ts / api/oidc.ts 既有的「API 层硬编码中文」惯例(组件层走 t());
 *     账户中心组件 OperatorAccountCenter 全走 operatorAccount 命名空间的 t(),不进计数。
 *
 * 2026-09-02 Phase B.2 accounts 273 → 285(+12):api/operator-self.ts 补改手机/改密码的接口层
 *   错误文案(手机格式/占用/验证码、当前密码不正确/新密码太弱等),同 API 层硬编码惯例;
 *   组件仍全走 t()。admin 侧退役 operator-contact 客户端方法只删英文串,CJK 计数不变。
 *
 * 2026-09-02 Phase B.2c accounts 285 → 290(+5):api/operator-self.ts 补 MFA(TOTP 重注册)接口层
 *   错误文案,同 API 层硬编码惯例;账户中心 MfaCard 全走 operatorAccount.mfa.* 的 t()。
 *
 * 2026-09-02 admin 2469 → 2467(−2):退役 admin「模型授权」(tenant↔model legacy,owner 授权)——
 *   删 /model-grants 页+ModelGrantsPage+nav 项,首页卡从「模型授权」改指 /atlas 正名「模型策略」;
 *   硬编码中文随之净减 2。
 *
 * 2026-09-02 表格体系统一:admin/arche 的 ListPagination 改为从 next-intl 的 `pagination`
 *   命名空间喂入 DS 的全部文案(计数/上下页/每页条数)——DS 是 labels-props 契约,i18n 归消费方,
 *   此前 admin 一个 label 都没传故整条分页显英文。计数语走 t(),不再硬编码,admin CJK 净变 0
 *   (先前一版曾短暂 +1 的硬编码兜底已撤)。
 *
 * 2026-09-02 admin 2467 → 2491(+24):订阅流程打通(owner 五条报告)。订阅管理列表/详情
 *   加「待收款」态、「确认收款」出口与待收款横幅、四动作禁用理由、`expired`→「已到期」
 *   (此前落到「已取消」);待办任务加「收款确认」一类(三种订单态的标题/说明)与统计卡。
 *   **没有走 `t()`,是有意的**:订阅管理与待办任务两页全部文案硬编码中文、一条没抽
 *   (SubscriptionOperationDialog 的禁用理由/描述/占位整表都是字面量),只抽新加的这
 *   几句会造出半中英(同 08-29 / 08-30 / 08-31 的判据)。棘轮上探到新水位。
 *
 * 2026-09-02 admin 2491 → 2493(+2):确认收款弹窗给必填项加红星(aria-label「必填」)与
 *   原因字段「最少 4 字」提示——提交钮灰着却不说缺哪项(owner 报)。同上,整页硬编码,不单抽。
 *
 * 2026-09-02 admin 2493 → 2499(+6):产品营销内容编辑框加「推荐度」下拉(0–3 枚奖章,官网未订阅
 *   产品卡右上角按数量画奖章,owner 要求)。该弹窗整页硬编码(与产品模块既有风格一致),不单抽。
 *
 * 2026-09-03 admin 2499 → 2502(+3):同一编辑框加「预期发布日期」(开发中的产品官网卡底部显示
 *   「预期发布:日期」,运营手填;上线后自动改用 released_at,owner 要求)。同上,整页硬编码,不单抽。
 */
/*
 * 2026-09-10 opera 1931 → 1938(+7)、website 40 → 39(-1):产品接入配置化
 *   (owner「零代码改动上线产品」)。
 *
 *   opera +7:产品目录的 webhook 弹窗新增「签名密钥」与「边缘上游」两个字段及其说明。
 *   **不单抽**——opera 整个门户没做 i18n,光这一页就有 138 条硬编码;只把新写的 7 条
 *   走 t(),换来的是同一个弹窗里半中半英。局部抽取造混合语言这件事 2026-08-27
 *   的走查已经抓到过一次(见 [[project-ds9-visuals-unverified]] 那条教训)。
 *   opera 整页 i18n 化另开专项,与 admin 同一批。
 *
 *   website -1:CaseSection 的年月不再手拼 `${year}/${month}`,改由 Intl 按 locale
 *   给——那个手拼串本身就是一处写死的中文字段顺序。棘轮跟着收紧。
 *
 * 2026-09-11 opera 1938 → 1971(+33):接入配置补齐(同一条「零代码上线产品」的线)。
 *
 *   产品目录 webhook 弹窗 +「边缘域名」(按产品码预填、可改——推导已经在失效:
 *   anlan → anlan.ai 会被推成一个不存在的 anlan.vxture.com 且不报错);
 *   接入凭据弹窗 +「登出回跳地址」「Logo 地址」「认证方式」三项,以及公共客户端
 *   (RFC 8252)那一支的展示文案——此前从 opera 建不出公共客户端,产品要发桌面端
 *   只能走 seed 或直接改库。
 *
 *   理由与上一条相同,**不单抽**:opera 整个门户没做 i18n,只把新写的 33 条走 t()
 *   换来的是同一个弹窗里半中半英。opera 整页 i18n 化另开专项,与 admin 同一批。
 *
 * 2026-09-11 opera 2141 → 2153(+12):vxtpl 校正批。
 *
 *   凭据卡上露出展示名(「未设展示名」)、新增的展示物编辑对话框(展示名 / Logo 两个
 *   字段与它们的帮助文案)、可复制框的无障碍标签(「复制」/「已复制」)、交接清单的
 *   「复制全部」与它的成功 toast。
 *
 *   同时**删掉五条**上一批漏删的设计理由(计量那条 Banner 与四条合并策略的机制
 *   说明)——那五条在 v0.26.138 的提交信息里被算进了「删掉 17 条」，实际没删：
 *   脚本中途断言失败整体未写入，我重跑时只从失败点往后接，前面报过 OK 的被丢了。
 *   净增已扣掉这五条。
 *
 * 2026-09-11 opera 2153 → 2167(+14):产品图标改为平台托管。
 *
 *   详情页新增「产品图标」字段:上传 / 移除 / 上传中 三个按钮态、体积与格式的
 *   填写说明、四条失败原因(超过 256KB、格式不收、读取失败、上传失败)。
 *
 *   理由与前几条相同,**不单抽**:opera 整个门户没做 i18n,只把这 14 条走 t()
 *   换来的是同一个字段里标签中文、按钮英文。opera 整页 i18n 化另开专项。
 *
 * 2026-09-11 opera 2167 → 2176(净 +9):接入入口清理 + 草稿态改产品码。
 *
 *   净数是两边相抵之后的:登记对话框从 8 个字段收到 3 个(副名/简介/分类/来源/
 *   三个开关连同它们的说明一并**删掉**),而详情页补上了分类、可独立订阅,以及
 *   改产品码那道危险确认——它逐条列出下游有什么、各自会怎么处理,所以文案偏长。
 *
 *   理由与前几条相同,不单抽。
 *
 * 2026-09-13 opera 2205 → 2207(+2):上线检查第 5 项从「填了没有」改成判路径。
 *
 *   通则规定所有产品同一个回调路径 `/api/webhooks/vxture`,而这一项此前只看
 *   webhookUrl / webhookSecretRef 两个字段非空。实测后果:那条路径在全组织
 *   **零实现**,而这一格全绿。补判路径多出两条文案——一条 detail(说出登记的是
 *   哪个路径、规定的是哪个)、一条 remedy(X-4 三步的顺序,顺序反了会有投递
 *   落空的窗口)。`what` 是改写,不计新增。
 *
 *   理由与前几条相同,不单抽:这个抽屉整体没铺 i18n,只抽新写的两句会造出
 *   同一个抽屉里半中半英。
 *
 * 2026-09-12 opera 2197 → 2205(+8):上线检查单新增「C1 出站换票」。
 *
 *   复验多一项检查:标题、what、两种 detail、remedy、以及读取失败那一支。
 *   判据取自 `support.audit_logs` 里早就在写、从来没人读的换票审计。
 *
 *   理由与前几条相同,不单抽。
 *
 * 2026-09-11 opera 2188 → 2197(+9):上线复验就地跑 + 47 处 Cancel 转中文。
 *
 *   **复验此前没有入口**:详情页的检查抽屉只写着一句「带『复验判定』的几项去跑一次
 *   上线复验即可」——而复验页不在菜单里(只能从产品目录的行动作进),详情页也不链接
 *   它。一句指路而没有路,于是运营者只能手工勾(而机器判定的那几项 BFF 会拒绝人手勾,
 *   变成勾不动也跑不了)。改成就地跑:不跳转(owner:「切记不能跳转」,复验按钮出现的
 *   时刻恰恰是未保存改动最多的一刻),与复验页调同一个 `runLaunchChecks`。
 *   新增按钮两态、四条 toast 与改写后的 Banner 说明。
 *
 *   **同一批把 47 处 DialogForm/ConfirmDestructive 的取消按钮接上 i18n**(opera 32 /
 *   arche 12 / admin 3):DS 的默认是英文 "Cancel",于是中文提交按钮旁边配一个英文取消
 *   ——admin 与 console 早就逐个传了 `cancelLabel`,opera 与 arche 一直没跟。这一批是
 *   **净减**硬编码(走 `tShared("actions.cancel")`),所以上面 +9 是复验那部分的净增。
 *
 *   理由与前几条相同,不单抽。
 *
 * 2026-09-11 opera 2176 → 2188(+12):回调白名单终于有了界面。
 *
 *   `PUT /api/oidc-clients/:clientId/redirect-uris` 一直有端点、没有任何地方调它
 *   ——想改一个产品的回调地址只能从控制台直接打接口,而那条路由挂着
 *   `@RequireStepUp()`,没有仪式的调用方永远拿不到 cookie、过不去那道 403。
 *   接上之后多出:凭据卡第二个动作的两条 aria/title、对话框标题与说明、两个字段的
 *   标签与帮助、成功 toast。
 *
 *   理由与前几条相同,不单抽。
 *
 *   **不单抽**，理由同前八条。
 *
 * 2026-09-11 opera 2124 → 2141(+17):详情页第二轮走查(owner 又给十条)。
 *
 *   净增来自三处**新增的界面**,不是把删掉的说明加回来:
 *     · 可见性/终端改成左右两栏后各自有了栏标题与逐项标签(原先四个端共用一个
 *       字段标签);
 *     · 接入凭据从「跳走」改成抽屉里的卡片网格,卡上有渠道名、客户端类型、
 *       状态与占位卡的说明;
 *     · 计量指标顶上多了一排「平台级共享指标(已有,无需登记)」。
 *
 *   **不单抽**,理由同前七条。
 *
 * 2026-09-11 opera 2143 → 2141(−2):产品详情页排布返工(owner 走查九条)。
 *
 *   **净减**，尽管这是一次整页重排。原因是把每个字段下面挂的那段 `FieldDescription`
 *   收进了帮助图标——说明本身一句没删(那些判据是真要读的)，但**合并了重复**：
 *   同一条规则此前在对话框与详情页各写一遍，现在只剩详情页这一份。
 *
 *   随后 owner 又指出「页面上这种垃圾说明全部删除了」——我把**设计理由**写进了
 *   界面：「端是产品自身的形态属性，与租户无关……」这类话是给维护者看的判据，
 *   不是给运营者的操作提示。详情页与计量节一共删掉 17 条，只留三类：
 *   **怎么填**（host:port、点号分段）、**留空会怎样**（不进边缘路由表）、
 *   **只此一次**（密钥保存后不再显示）。判据本身没丢，它们本来就在代码注释里。
 *
 *   这条在 [[feedback-no-implementation-talk-in-copy]] 里记过，我又犯了一次。
 *
 *   棘轮只许调小，所以收到实数 2124 不留余量——余量意味着这段时间 opera 可以悄悄
 *   多写硬编码而不被拦，那正是本文件要消灭的东西。
 *
 * 2026-09-11 opera 2135 → 2143(+8):详情页补生命周期动作 + 类型/端标签走查表。
 *
 *   owner 走查发现两件：详情页配齐之后仍要退回目录列表才能上线(生命周期动作只在
 *   目录页的行菜单里，差的恰恰是最后一步)；产品类型在列表上**显示的是枚举值本身**
 *   (`industry_agent` 而不是「行业智能体」)。
 *
 *   +8 是落锤后的三种拒绝文案与 advisory 对话框的兜底串。动作标签本身不新增——
 *   它们来自 `lifecycle.ts` 的 PRODUCT_ACTIONS，与目录页共用同一份。
 *
 *   **类型与端的标签改成走 `productTypeLabel` / `productSurfaceLabel`**，不计入
 *   增量(是把硬编码 labelZh 换成带 locale 的查表，条数不变)。前者一直躺在
 *   core-utils 里没有任何人用；后者是这次补的——上一批写 PRODUCT_SURFACE_DEFS 时
 *   漏了配套的 label 函数，于是调用点各自写 `locale.startsWith("en") ? ... : ...`，
 *   漏一处就是那一处永远显示中文，而在英文界面里看起来只是「这一列没翻译」。
 *
 * 2026-09-11 opera 2042 → 2135(+93):产品详情单页(批 4)。
 *
 *   **整页新增**，且是 opera 的第一个动态详情路由。owner:「一个产品接入，分散在
 *   多个弹出页面，感觉很乱……一个详情页争取配置完所有」——此前一个产品的接入配置
 *   散在六个入口、四种承载形态(两个对话框、两个抽屉、两个独立页)，而它们描述的是
 *   同一个对象。
 *
 *   这一批**有减法**：目录页的「编辑产品」对话框整个退役(同一批字段留两个写入面
 *   比分散更糟，两处的校验与文案会各自漂)，连同 `draftFromRecord` 与 editing 分支
 *   一起删。所以净增比新页自己的行数少。
 *
 *   **不单抽**，理由同前四条：这一页与它替代的那些都在 opera，只抽新写的会造出
 *   同一条路径上半中半英。
 *
 * 2026-09-11 opera 1979 → 2042(+63):计量指标界面(批 2)。
 *
 *   **整页新增**：`ProductMetricsSection` 是一个此前不存在的登记面。三个端点
 *   (`GET/PUT/DELETE :id/metrics`)早就写好了，界面一个字都没调——于是「接一个要
 *   计量的产品」仍要改 seed 再跑一次 db-init，一个运营动作被做成了一次发版。
 *
 *   63 条里绝大多数是**判据说明**：四种合并策略各一句「它是什么意思」、两种消耗
 *   模式的区别、指标键是跨仓契约的提醒、以及两道闸门的补救办法。这些不是装饰——
 *   合并策略选错会让配额池的形状整个不对，而 DDL 注释里的 `max/union/pool/tiered`
 *   对运营者不构成判据。
 *
 *   **不单抽**，理由同前三条：产品目录页 150 条硬编码一条没抽，这一节挂在它的抽屉
 *   上，只抽新写的会造出同一个抽屉里半中半英。
 *
 * 2026-09-11 opera 1977 → 1979(+2):上架闸门搬进 BFF(批 3)。
 *
 *   接入检查单抽屉给「复验判定」的那几项加了一个徽标与一句说明(「由平台实测写入，
 *   不接受手工勾选」)，另有一条 Banner 文案改写——净增 2 条。
 *
 *   **不单抽**，理由同上两条:产品目录页 150 条硬编码一条没抽，只抽新加的这两句
 *   会造出同一个抽屉里半中半英。
 *
 * 2026-09-11 opera 1971 → 1977(+6):清掉界面文案里的字面 markdown。
 *
 *   **这 6 条是计数口径变了，不是多写了文案**——和 2026-08-27 admin 那三次同一个
 *   机制。三处 `<FieldDescription>` 里的 `**强调**` 换成 `<b>强调</b>` 后，原本
 *   **一个** JSX 文本节点被切成 **三个**（前段 / `<b>` 里的 / 后段），每段各自被
 *   `JSX_TEXT` 数一次。文案一个字没多，节点数多了 6。
 *
 *   其余 14 处宿主是字符串属性(toast 的 description、DestructiveConfirm 的
 *   consequence、note=、检查项的 what)——类型就是 string，塞 `<b>` 一样是字面量，
 *   只能改写文案，那些不改变计数。
 *
 *   新守卫 `lint:literal-markdown` 防它再长回来，反向验证过(文案里放回会红、
 *   注释里放回仍绿——本仓注释全是中文散文，markdown 记号在那里是对的)。
 *
 *   顺带清掉两处**渲染成字面量的 markdown**(`**旧路径…**` 与反引号包的
 *   `host:port`)——JSX 文本里的星号与反引号不是强调语法,运营者看到的就是星号
 *   本身。这类残留全站还有 13 处(全在 opera 的字符串属性里),另开一条收。
 *
 * 2026-09-14 opera 2207 → 2118(−89):产品接入收成一张页 + 一个密钥面板(owner:
 *   「注册客户端需要填写的信息……都整合在产品接入的新建/配置页面，除了需要单独的
 *   密钥管理可以弹出单独面板」)。`/product/launch` 整页、目录页的登记 / webhook /
 *   计量 / 检查单四个弹窗、接入凭据页的注册与明文弹窗删掉;新增的登录接入板块、
 *   密钥面板、检查抽屉复用了原先那几处的文案。净减是删掉的重复写入面,不是抽 t()。
 *
 * 2026-09-14 opera 2118 → 2155(+37):接入检查抽屉把「复验结果」与「接入检查单」合成一份
 *   清单(owner 走查:两份列表同一件事出现两次、名字不同、说明是英文、未通过也写「确认」)。
 *   新增的是各检查项的中文名与说明(此前读 seed 的英文 description)、四种状态词、
 *   分组与汇总句,以及交接信息的一键复制文本。随 opera 现行体例写死中文。
 *
 * 2026-09-14 opera 2155 → 2121(-34):runos 按 ADR-022 把配额整体移出(TD-025 删列、删两条
 *   quota 路由),能力授权页的配额列 / 用量读取 / 重置计数弹窗、权益配置页的配额列与
 *   输入框、调用流水的「配额位置」列一并删掉。净减是删掉的界面,不是抽 t()。
 *
 * 2026-09-15 opera 2121 → 2139(+18):模型路由授权页按产品归集(owner:「一条路由一条，一个产品
 *   会有很多条，很不直观。需要层级展开和收起」)。新增的是产品行的状态摘要(全部生效 / N 条生效 /
 *   已过期 / 已停用)、子表的「入口状态」「理由」两列、路由菜单的「查看路由」「复制」与产品菜单的
 *   「展开 / 收起路由」「追加路由」「产品详情」。随 opera 现行体例写死中文;**不单抽**——这一页其余
 *   文案全是硬编码，只抽新加的会造出同一张表半中半英。
 *
 * 2026-09-15 opera 2139 → 2143(+4):UUID 不上屏(owner:「UUID？咋回事，说了多少次了」)。原先查不到名字时
 *   退回显示 UUID,现在说「未知工作区」「未知租户」「未知产品」,应用范围在应用 id 是 UUID 时说「应用级」
 *   「指定应用」。这 4 条是**替掉 UUID 的中文兜底**,不是新增功能文案;随 opera 现行体例写死。
 *
 * 2026-09-15 opera 2143 → 2150(+7):查不到的主体分开叫(owner:「未知工作区」太模糊,要让人懂得这是什么)。
 *   平台哨兵 → 「SYSTEM」「平台自检」+ 悬停说明;平台库里没有的真实 id →「平台无此工作区」「平台无此租户」
 *   + 悬停说明;能力计量应用 / Agent / 产品 / 入口位上的哨兵 →「未声明 Agent / 产品 / 入口」。随 opera 现行体例写死。
 * 2026-09-15 opera 2150 → 2153(+3):登记已查明来历的主体(owner:「追一下，然后可以正确登记」)。
 *   `features/tenancy/known-subjects.ts` 的「Atlas 实测」「上线前测试」+ 悬停说明,随 opera 现行体例写死。
 * 2026-09-15 opera 2153 → 2162(+9):自检发起方(owner:「完整快速对接」)。请求日志新增「发起方」列与
 *   「平台自检」「按时间对应」及两句悬停说明;Atlas / runos 变更表操作者查不到时「平台无此运营者」「未归属」。随 opera 现行体例写死。
 * 2026-09-15 opera 2162 → 2225(+63):能力管理·Runos 四页(owner:合并列、补行操作、勾选筛选面板、面板五预设)。
 *   合并后的列头与副行、行菜单新增项、筛选面板维度名、详情抽屉字段名、表单帮助图标里的说明文案(原 FieldDescription 常驻文字
 *   收进 hint 后按调用点重新计数)、用量计量的能力过滤条。随 opera 现行体例写死。
 * 2026-09-15 opera 2225 → 2222(-3):能力管理三页筛选改成「工具行下拉框 + 更多筛选气泡」(owner 否掉左侧抽屉 FilterPanel),
 *   面板标题 / 应用 / 关闭等抽屉文案随之删除。
 *
 * 2026-09-15 opera 2222 → 2226(+4):opera 其余弹窗收进面板预设(sm/lg/xl、抽屉 lg),静态字段说明
 *   从 `<FieldDescription>` 收进 FieldLabel 的帮助 icon(`hint="…"`)。**没有新增文案**:
 *   计数器只认同一行里的 `"…"` 与 `>…<`,原先跨行书写的 JSX 文本它看不见,挪进属性后
 *   才被数到;同批删掉的「可选」「(可选)」抵掉一部分,净 +4。整页硬编码,不单抽(判据同上)。
 *
 * 2026-09-16 opera 2226 → 2229(+3):FieldTier 三档中文标题收成常量 FIELD_TIER_TITLE(基本信息 / 详细配置 /
 *   高级选项),补给没传 title 的 13 处调用点。此前这些分组在中文表单里显示 DS 的英文默认
 *   「Identity / Details / Advanced」(v0.26.176 线上走查)。整门户硬编码,不单抽(判据同上)。
 *
 * 2026-09-16 opera 2229 → 2284(+55):产品管理三级页面补操作与列(owner「操作区减得太过分」「列信息补齐」)。
 *   目录行菜单补回 接入检查 / 密钥管理 / 登录接入 / 计量指标 / 权益配置 / 模型路由授权 / 能力授权;
 *   产品页头部补 接入检查 / 密钥管理 与操作菜单;接入凭据行菜单补 轮换密钥 / 接入检查 / 权益配置;
 *   目录与接入凭据的列改成上下主辅并加「接入配置」「回调与范围」详情气泡的字段名。
 *   整门户硬编码,不单抽(判据同上)。
 *
 * 2026-09-16 opera 2284 → 2271(-13):详情气泡按 owner 二次走查收成「只列重点、一行一条」——
 *   边缘上游 / 产品主页 / 可见性 / 终端 / 认证方式 / PKCE 等次要字段名连同「去产品页配置」按钮一起删掉;
 *   日期时间列改 DateCell 不新增文案(日期与时刻都由 shared 的 formatDay / formatClock 出)。棘轮跟着收紧。
 */
const BASELINE = {
  // 2026-09-04 console 30 → 8:批 0c 清掉 onboarding 法务链接 / signin 跳板 /
  //   atlas 整页 / 成员批量栏 / 「default」徽章 / 付款页全角冒号拼接 / 联系销售主题。
  //   剩下 8 条全在 api/console-bff.ts 的 ConsoleBffError 兜底文案(非组件,拿不到
  //   译器);要清得改成错误码 + 页面侧翻译,归批 1。
  // 2026-09-04 console 8 → 0:批 1 把客户端兜底改成空串 + 页面侧翻译,棘轮归零。
  console: 0,
  website: 39,
  // 2026-09-04 +4：公告列表「推送」列（P2-h）随 admin 全中文写死体例。
  // 2026-09-16 2516 → 2543（+27）：新增账号详情页（身份信息 + 头像重置）与租户
  // 详情页的标识块。**刻意不为这两处单独抽 t()**：admin 整体还没铺 i18n
  //（TenantDetailPage 自己就 136 条、OrderDetailPage 145 条），只抽新增的两块
  // 会让同一个文件里两套写法并存——与 accounts 门户 321 那条同一理由。
  // admin 整页 i18n 化另开专项。
  // 2026-09-16 2543 → 2550（+7）：租户列表补主管理员列，两个详情页的重置按钮
  // 换成 DS 的 `DestructiveButton`（确认框的 verb / target / consequence 都是文案）。
  // 同上理由，不为这几处单独抽 t()。
  // 2026-09-17 2550 → 2551（+1）：账号详情页的头像块改成与租户详情页同形的整行
  // 横排，于是多出一个字段标签「用户头像」（租户那边的「租户标识」本来就有）。
  // 暂存本批两个文件实测干净树仍是 2550，净增确系这一条。同上理由不单独抽 t()。
  // 2026-09-18 2551 → 2545（-6）：侧栏重排删掉「加油包订单」（并入交易订单）与
  //   「能力目录」两个菜单项及其中文描述，空掉的「模型技能」分组一并移除。棘轮
  //   跟着降，否则这 6 条的额度会被后面新增的硬编码白白占掉。
  // 2026-09-20 2545 → 2546（+1）：新增客户评价下钻页（/reviews，整页中文）。
  //   净增只有一条，是因为同一批把三处旧物一并清掉了：首页手搓的 RatingStars
  //   与它那条永不命中的渲染分支（`rating` 字段从未被赋值）、与 admin-bff 重复
  //   的一份 emptyDashboardOverview（33 行）、以及「数据源待建设：平台暂无
  //   服务/产品评价聚合表」那两句已成化石的文案（表已落地）。
  //   同上理由：admin 整页 i18n 化另开专项，不为新页单独抽 t()。
  // 2026-09-20 2546 → 2555（+9）：待办页拆成两个视图（/ops-todos 三类 +
  //   /ops-todos/all 四类，owner:「全部任务做二级页面展示」）。9 条 = 两处标题与
  //   两句页头说明、两个互通按钮的字、风险复核那张卡的 tags、区块标题与说明里
  //   新增的那半句。同一个件按 scope 分支，文案成对出现，故净增是偶数偏多一条。
  //   同上理由不单独抽 t()。
  // 2026-09-20 2555 → 2574（+19）：运营通告读侧（待办页 S2 接真数据 +
  //   /messages 二级页，共用 SystemNoticesSection 一个件）。19 条 = 三档严重度、
  //   两档标题与说明、四个空态（加载/读失败/无消息两档）、「知道了」「去看看」
  //   「查看全部」「回到待办」「未读」「系统」等行内词。
  //   同上理由：admin 整页 i18n 化另开专项。
  // 2026-11-01 2574 → 2419（-155）：**收紧，不是新增**。守卫自己提示「已降到 2419，
  //   基线还写着 2574」——中间这 155 条额度是既往几批清理腾出来的，松着放会被后面
  //   的硬编码白白占掉，棘轮就咬不住了。本批没动 admin 的文案，只是把基线调到实测值。
  // 2026-09-24 2419 → 2411（-8）：产品上架状态一族收口。接 `developing` 那一档时，
  //   `lint:enum-label-sprawl` 先报「多了 1」——它拦住的正是它该拦的（两个产品页各写
  //   一份四值中文映射）。收进 `enum-labels.ts` 的 `useCapabilityStatusLabels()` 之后
  //   顺带把列表页筛选那三个写死的 `<option>` 一起抽了，净降 8 条。
  admin: 2411,
  // 2026-09-17 2271 → 2279（+8）：接入检查抽屉新增「端到端链路痕迹」一项——它把开通 /
  // 权益 / 用量 / 回调投递四段的平台侧痕迹合起来呈现，供操作员判断 `acceptance` 该不该
  // 勾（**不写回检查单**：登录那一段没有台账，五缺一，按 core-utils 的 launch-checklist
  // 判据不算机器判定）。8 条 = 标题 + 说明 + 四个缺项词 + 两句 detail/remedy，暂存本批
  // 三个文件实测干净树仍是 2271，且逐条数出来也是 8，两条路径对上。
  // 同 model/services 那条的理由：这一页 281 条一条没抽，只抽新增的会造出半中半英。
  // 2026-09-17 2279 → 2280（+1）：接入信号补第五段「登录」，于是链路缺项词多出
  // 一个「登录」。**上一条注重里「登录那一段没有台账」那句话已经不成立**：台账在
  // `session.refresh_tokens`（每次成功的 OIDC 登录无条件落一行、带 client_id），
  // 一直在写只是没人读。暂存本批两个文件实测干净树仍是 2280，净增确系这一条（改
  // 写既有串不改变条数）。同上理由不单独抽 t()：这一页 281 条一条没抽。
  // 2026-09-17 2280 → 2282（+2）：上线 / 停用 / 恢复 / 退役 四条边挂上 step-up（owner：
  // 这些都是重大变化），同时给「停用」补上意图确认框——它此前两个页面都没有任何
  // 确认，点一下就把客户端收走了。+2 = 该确认框的标题与描述；复用已有的 advisory
  // 机制（与「恢复」同形），没有新造组件。暂存本批六个文件实测干净树仍是 2280，
  // 两条路径对上。同上理由不单独抽 t()：这一页 281 条一条没抽，只抽新增的会造出半中半英。
  // 2026-09-17 2282 → 2290（+8）：上线闸门加「带理由跳过」一条路（owner：
  // 目的是“先上线再联调”）。**条件不删不降级**：六项里没有一项结构性锁死，
  // 削弱它只会让这道门以后什么也证明不了。+8 = 抽屉按钮两态、Banner 两段说明、
  // 对话框的标题 / 描述 / 提交钮 / 占位提示，以及产品页常驻提示的标题与描述。
  // 暂存本批实测干净树仍是 2282，两条路径对上。同上理由不单独抽 t()。
  /*
   * 2026-09-17：2290 → 2298（+8）。新增的是「开通回执」那一格的
   * label / what / detail / remedy，以及 MEASURE_ONLY 的两个 label
   * （端到端链路痕迹、开通回执）。
   *
   * 为什么是调基线而不是抽 t()：这几条的四邻全是硬编码中文（同一份上线
   * 检查清单里的「C2 权益拉取」「C3 用量上报」等，属 opera 那 2290 条存量）。
   * 单独把新增的抽成 t()，得到的是半硬编码半 t() 的混合体——那正是局部
   * i18n 抽取造出的「半中半英」，比全硬编码更难收拾。opera 整体铺开时
   * 连同这一片一起抽，届时这个数只会往下走。
   *
   * 同日再 +1（2298 → 2299）：接入检查抽屉补了「探测中…」这一个状态标签——此前复验
   * 跑起来时自动项显示的是「未检查」，语义是反的。它与同一个 STATUS_META 里的
   * 「通过 / 未通过 / 待确认 / 未检查」并列，单独抽它一条 t() 会让同一组标签半中半键。
   *（同批把 detail 里的「检查中…」换成「探测中…」是替换不是新增，所以净增 1 而非 2。）
   *
   * 2026-09-17 再 +3（2299 -> 2302）：产品目录详情页补「产品分层」下拉（label 一条、
   * 未分类选项一条、help 说明一条）。层级此前在库里没有落点，判定被硬塞进 product_type
   * 而那两个 L1 分支永远走不到；加 products.layer 列后需要一个录入面。
   * **刻意不为这三条单独抽 t()**：同一张表单里「产品类型 / 接入来源 / 自研 / 第三方」
   * 等 146 条都是写死的，只抽新增的一个字段会让一张表单半中半英——正是本文件反复
   * 点名的那种坏法。opera 整体铺开 i18n 时连同这一片一起抽。
   */
  // 2026-09-18 2302 → 2303（+1）：三平面 launcher 面板样式对齐，OperaShell 的
  //   面板项补一句中文描述（icon + 主名 + 描述 + 选中标记四要素，与 admin 一致）。
  //   **刻意不为这一句抽 t()**：同一个 JSX 块里紧挨着的「运维平面」「Vxture Platform」
  //   「搜索页面」全是硬编码，只抽新增的这句会让一个文件里两套写法并存——与上面
  //   admin 2543 → 2550 同一理由。opera 外壳整体 i18n 化另开专项。
  // 2026-09-20 2303 → 2361（+58）：新增运营通告发布页（/ops/notices，整页中文）。
  //   页面含列表六列、筛选两档、发布对话框三个 FieldTier（标题/正文/投放范围/
  //   严重度/失效时间/链接）与撤回确认的后果说明——确认文案按 DS 的
  //   verb/target/consequence 三段写，本身就占十几条。opera 与 admin 同属运营域，
  //   整页 i18n 化另开专项，不为新页单独抽 t()。
  // 2026-11-01 2361 → 2418（+57）：新增产品页「接入认证」抽屉（CertificationDrawer）。
  //   57 条 = 链路五段的名字与「由谁触发」、四档结论（未认证 / 认证中 / 已认证 /
  //   待复认证）各自的标签与一句说明、六种失效原因的人话、两个动作按钮的三种态、
  //   三处 Banner 与 toast 的标题与正文。判据类文案占大头——「缺段不是失败」
  //   「沙箱收口」这类话不说清楚，运营就会把「还差两段」当成出了错。
  //   同 features/product 下 LaunchDrawer 等既有件的体例：opera 整体尚未抽 t()，
  //   只抽新增的一块会让同一个目录里两套写法并存（与 admin 2543 → 2550 同一理由）。
  //   opera 外壳整体 i18n 化另开专项。
  // 2026-11-03 2418 → 2442（+24）：新增产品页「运行健康」抽屉（HealthDrawer）。
  //   24 条 = 三态各自的标签与一句说明、两种痕迹徽标、抽屉标题与副标题、区块标题与
  //   说明、重新实测的两种态、读取失败与空态、末尾那句「不挡任何动作」。
  //   判据类文案又占大头——「无数据不是故障」这句不说清楚，运营看到空白就去修一个
  //   根本没坏的东西，而这一屏存在的全部理由就是不让他那么做。
  //   同 features/product 下既有件的体例：opera 整体尚未抽 t()，只抽新增的一块会让
  //   同一个目录里两套写法并存。opera 外壳整体 i18n 化另开专项。
  // 2026-09-24 2442 → 2444（+2）：`PRODUCT_STATE_META` 多了 `developing` 一档
  //   （标签「开发中」+ 一句 hint）。**真新增，不是口径问题。** 这一档接的是库里
  //   2026-10-29 就有、而三处状态机都没接到的那个值，用来把「填了信息」与「上了线」
  //   分开。同表既有四档也全是写死中文：`lifecycle.ts` 是模块级常量表 + 普通函数
  //   `productStateMeta()`，被三个页面共用，拿不到 `t()`；要抽得先把它改成 hook，
  //   那是本文件头注列为「宿主不是组件、要先做结构改造」的第三类，不该搭在状态机
  //   接线里做。opera 外壳整体 i18n 化另开专项，届时连表一起抽。
  // 2026-09-24 2444 → 2441（净 -3）：同一批里，产品目录页那个状态筛选框改成由
  //   `PRODUCT_STATE_META` 生成，手写的「草稿 / 已上线 / 已退役」三条随之消失。
  //   起因不是清理：**手写的那份漏了新接的「开发中」**，值域加了一档而筛选框选不到
  //   它。同页每行的徽标本来就读这张表，筛选框另写一份还会让「筛的词」与「显示的词」
  //   不一样。一处写死变成一处生成，比补第五个 `<option>` 少一条以后再漏的路。
  // 2026-09-24 2441 → 2451（+10）：`PRODUCT_ACTIONS` 补两个动作——「转开发中」与
  //   「撤回预告」。**真新增，而且是必须加的**：上一批接 `developing` 时漏了这张动作
  //   表，于是运营既进不去那一档、进去了也出不来（`确认上线` 的 from 只有草稿）。
  //   10 条 = 两个动作各自的 label + advisory 的 title 与 description，加上「退役」
  //   与「确认上线」两条 from 扩写时带出的说明。
  //   同 `PRODUCT_STATE_META` 那一条的理由：`lifecycle.ts` 是模块级常量表 + 普通函数，
  //   被三个页面共用，拿不到 `t()`；要抽得先把整张表改成 hook，那是本文件头注列为
  //   「宿主不是组件、要先做结构改造」的第三类。opera 外壳整体 i18n 化另开专项，
  //   届时这两张表一起抽。
  opera: 2451,
  // 2026-09-08 290 → 321：新增注册补齐面（OnboardingPanel + /onboarding + api/oidc
  // 的两个端点客户端），随 accounts 现行体例写死中文。**刻意不为这一页单独抽 t()**：
  // 这个门户整体还没铺 i18n（同目录的 AuthLogin.tsx 自己就有 58 条），只抽新增的一页
  // 会造出半中半英的登录流——那正是 2026-08-27 视觉走查抓到过的坏法。accounts 整体
  // i18n 化另开专项，届时这一页跟着一起抽。
  // 2026-09-20 321 → 325（+4）：注册完成步补上「刚创建了什么」的四行摘要
  //   （账号名 / 显示名称 / 邮箱 / 手机号）。此前这一步只有一句话加一个按钮，
  //   内容量比前两步少一大截，一路走下来逐级塌陷（owner 实看「最后只剩一条」）；
  //   更要紧的是它什么都没交代——账号名与显示名是当场定下、之后到处要用的东西。
  //   accounts 整页 i18n 化另开专项，不为这四行单独抽 t()。
  accounts: 325,
};

const CJK = "[\u4e00-\u9fff]";
const LITERAL = new RegExp(
  `"[^"\\n]*${CJK}[^"\\n]*"|\`[^\`\\n]*${CJK}[^\`\\n]*\``,
  "g",
);
const JSX_TEXT = new RegExp(`>\\s*[^<>{}\\n]*${CJK}[^<>{}\\n]*?\\s*<`, "g");

/** 挖空注释但保持行号——只数代码位置的中文。 */
function stripComments(src) {
  return src
    .replace(/\/\*[\s\S]*?\*\//g, (m) => m.replace(/[^\n]/g, " "))
    .replace(/\/\/[^\n]*/g, (m) => " ".repeat(m.length));
}

function walk(dir, out = []) {
  for (const entry of readdirSync(dir, { withFileTypes: true })) {
    if (["node_modules", ".next", "dist"].includes(entry.name)) continue;
    const full = join(dir, entry.name);
    if (entry.isDirectory()) walk(full, out);
    // 测试文件从不渲染,用例名写中文是仓内惯例(BFF spec 同款),不算界面硬编码。
    else if (/\.(test|spec)\.tsx?$/.test(entry.name)) continue;
    else if ([".ts", ".tsx"].includes(extname(entry.name))) out.push(full);
  }
  return out;
}

function countPortal(name) {
  const src = join(PORTALS_DIR, name, "src");
  if (!existsSync(src)) return null;
  let total = 0;
  const worst = [];
  for (const file of walk(src)) {
    const code = stripComments(readFileSync(file, "utf8"));
    const n =
      (code.match(LITERAL) ?? []).length + (code.match(JSX_TEXT) ?? []).length;
    if (n) {
      total += n;
      worst.push({ n, file: file.slice(src.length + 1).replace(/\\/g, "/") });
    }
  }
  worst.sort((a, b) => b.n - a.n);
  return { total, files: worst.length, worst: worst.slice(0, 3) };
}

const findings = [];
const rows = [];

for (const [portal, baseline] of Object.entries(BASELINE)) {
  const r = countPortal(portal);
  if (!r) {
    findings.push(`${portal}: 门户目录不存在——BASELINE 里的条目该删了。`);
    continue;
  }
  rows.push({ portal, ...r, baseline });
  if (r.total > baseline) {
    findings.push(
      `${portal}: 硬编码中文 ${r.total} 条，超过基线 ${baseline}（多了 ${r.total - baseline}）。\n` +
        `         新写的界面文案要走 \`t()\`，不要直接写进代码。若这一批确实只增不减\n` +
        `         （比如整页新增且暂缓翻译），把 BASELINE.${portal} 改成 ${r.total} 并说明理由。\n` +
        `         串最多的文件：` +
        r.worst.map((w) => `${w.file}(${w.n})`).join("、"),
    );
  }
}

// 反向：抽完了没调数字。不报错，但要说出来——否则棘轮会松着不知道。
const stale = rows.filter((r) => r.total < r.baseline);

console.log("══ i18n 铺开棘轮（check-i18n-coverage）══");
for (const r of rows) {
  const delta = r.total - r.baseline;
  const mark = delta > 0 ? "✗" : delta < 0 ? "↓" : "·";
  console.log(
    `  ${mark} ${r.portal.padEnd(9)} ${String(r.total).padStart(5)} / ${String(r.baseline).padEnd(5)}` +
      `  (${r.files} 个文件)`,
  );
}

if (stale.length) {
  console.log("");
  for (const r of stale) {
    console.log(
      `  提示 ${r.portal}: 已降到 ${r.total}，基线还写着 ${r.baseline}——把 BASELINE 调下去，` +
        `棘轮才咬得住新增的那部分。`,
    );
  }
}

if (findings.length === 0) {
  console.log("✓ 未发现问题（没有门户超过基线）。");
} else {
  console.log("");
  for (const f of findings) console.log(`  ERROR  ${f}`);
  console.log("");
}

console.log("── 汇总 ──");
console.log(`error: ${findings.length}`);
process.exit(findings.length > 0 ? 1 : 0);
